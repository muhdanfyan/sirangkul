#!/usr/bin/expect -f
set timeout 60
set host "89.233.105.92"
set user "sirangkul"
set pass "Sfgxjs4H38DQb7K"

# Check status code and peek into content
spawn ssh -o StrictHostKeyChecking=no $user@$host "curl -s -D - https://sirangkul.man2kotamakassar.sch.id/api/public/rkam/options | head -n 30"
expect "password:"
send "$pass\r"
expect eof
