#!/usr/bin/expect -f

set timeout 30
set user "sirangkul"
set host "89.233.105.92"
set password "Sfgxjs4H38DQb7K"

spawn ssh -o StrictHostKeyChecking=accept-new $user@$host
expect "password:"
send "$password\r"

expect -re "(\\$|#) $"
send "echo '-- TEST RKAM VIA HTTPS --'\r"
expect -re "(\\$|#) $"
send "curl -sk -o /tmp/rkam_test.json -w 'HTTP_CODE:%{http_code}' https://sirangkul.man2kotamakassar.sch.id/api/rkam -H 'Accept: application/json' -H 'Authorization: Bearer 23|XCN99wKMT6AZ8XgnOTqiDuZZmRAYhFNesXOwCmd2c5be0191'; echo ''\r"

expect -re "(\\$|#) $"
send "echo '-- TEST RKAM OPTIONS --'\r"
expect -re "(\\$|#) $"
send "curl -sk -o /tmp/rkam_options.json -w 'HTTP_CODE:%{http_code}' https://sirangkul.man2kotamakassar.sch.id/api/rkam/options -H 'Accept: application/json' -H 'Authorization: Bearer 23|XCN99wKMT6AZ8XgnOTqiDuZZmRAYhFNesXOwCmd2c5be0191'; echo ''\r"

expect -re "(\\$|#) $"
send "echo '-- NEW ERRORS AFTER FIX? --'\r"
expect -re "(\\$|#) $"
send "grep 'production.ERROR' /home/sirangkul/apps/sirangkul/api-sirangkul/storage/logs/laravel.log | grep '2026-04-16 07:5[6-9]\\|2026-04-16 08:' | tail -5\r"

expect -re "(\\$|#) $"
send "echo '-- RKAM RESPONSE SAMPLE --'\r"
expect -re "(\\$|#) $"
send "head -c 500 /tmp/rkam_test.json; echo ''\r"

expect -re "(\\$|#) $"
send "exit\r"
expect eof
